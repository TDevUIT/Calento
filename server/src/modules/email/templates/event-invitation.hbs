<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            line-height: 1.6;
            color: #000;
            margin: 0;
            padding: 0;
            background: #fff;
        }
        .wrapper {
            max-width: 600px;
            margin: 60px auto;
            padding: 0 40px;
        }
        h1 {
            font-size: 24px;
            font-weight: 600;
            color: #000;
            margin: 0 0 30px 0;
        }
        h2 {
            font-size: 20px;
            font-weight: 600;
            color: #000;
            margin: 0 0 20px 0;
        }
        p {
            margin: 0 0 20px 0;
            color: #000;
        }
        .organizer {
            border: 1px solid #000;
            padding: 20px;
            margin: 25px 0;
            display: flex;
            align-items: center;
        }
        .organizer-avatar {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            margin-right: 15px;
            background: #000;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-weight: bold;
        }
        .organizer-name {
            font-weight: 600;
            margin: 0 0 4px 0;
        }
        .organizer-email {
            color: #666;
            font-size: 14px;
            margin: 0;
        }
        .card {
            border: 1px solid #000;
            padding: 30px;
            margin: 30px 0;
        }
        .detail {
            margin: 15px 0;
            padding-bottom: 15px;
            border-bottom: 1px solid #e0e0e0;
        }
        .detail:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }
        .detail strong {
            display: block;
            margin-bottom: 5px;
        }
        .attendee {
            padding: 6px 0;
            display: flex;
            justify-content: space-between;
        }
        .attendee-email {
            color: #666;
            font-size: 14px;
        }
        .badge {
            padding: 4px 12px;
            border: 1px solid #000;
            font-size: 12px;
        }
        .button {
            display: inline-block;
            padding: 12px 24px;
            background: #000;
            color: #fff;
            text-decoration: none;
            margin: 10px 10px 10px 0;
        }
        .button-secondary {
            background: #fff;
            color: #000;
            border: 1px solid #000;
        }
        .notice {
            border-left: 3px solid #000;
            padding: 15px 20px;
            margin: 20px 0;
            background: #f5f5f5;
        }
        .notice p {
            margin: 5px 0;
            font-size: 14px;
        }
        .divider {
            height: 1px;
            background: #e0e0e0;
            margin: 40px 0;
        }
        .footer {
            color: #999;
            font-size: 12px;
            margin-top: 40px;
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <h1>Event Invitation</h1>
        
        <div class="organizer">
            {{#if organizerAvatar}}
            <img src="{{organizerAvatar}}" alt="{{organizerName}}" class="organizer-avatar" />
            {{else}}
            <div class="organizer-avatar">{{organizerInitials}}</div>
            {{/if}}
            <div>
                <p class="organizer-name">{{organizerName}}</p>
                <p class="organizer-email">{{organizerEmail}}</p>
            </div>
        </div>

        <p>Hi {{guestName}},</p>
        <p>You're invited to this event:</p>
        
        <div class="card">
            <h2>{{eventTitle}}</h2>
            
            <div class="detail">
                <strong>When</strong>
                {{eventStartTime}}
                {{#if eventEndTime}}
                - {{eventEndTime}}
                {{/if}}
            </div>
            
            {{#if eventLocation}}
            <div class="detail">
                <strong>Where</strong>
                {{eventLocation}}
            </div>
            {{/if}}

            {{#if conferenceUrl}}
            <div class="detail">
                <strong>Online Meeting</strong>
                <a href="{{conferenceUrl}}">Join Meeting</a>
            </div>
            {{/if}}
            
            {{#if eventDescription}}
            <div class="detail">
                <strong>Description</strong>
                {{eventDescription}}
            </div>
            {{/if}}

            {{#if showAttendees}}
            <div class="detail">
                <strong>Attendees ({{attendeeCount}})</strong>
                {{#each attendees}}
                <div class="attendee">
                    <span class="attendee-email">{{this.email}}</span>
                    {{#if this.responseStatus}}
                    <span class="badge">
                        {{#ifEquals this.responseStatus "accepted"}}Going{{/ifEquals}}
                        {{#ifEquals this.responseStatus "tentative"}}Maybe{{/ifEquals}}
                        {{#ifEquals this.responseStatus "needsAction"}}Pending{{/ifEquals}}
                    </span>
                    {{/if}}
                </div>
                {{/each}}
            </div>
            {{/if}}
        </div>
        
        <p>Will you attend?</p>
        <a href="{{acceptUrl}}" class="button">Accept</a>
        <a href="{{tentativeUrl}}" class="button button-secondary">Maybe</a>
        <a href="{{declineUrl}}" class="button button-secondary">Decline</a>

        <div class="notice">
            <p>Calento users: Event auto-added to calendar</p>
            <p>Guest users: You'll receive calendar invite (.ics)</p>
        </div>

        <div class="divider"></div>
        
        <p>The Calento Team</p>
        
        <div class="footer">
            <p>© {{year}} Calento</p>
        </div>
    </div>
</body>
</html>
