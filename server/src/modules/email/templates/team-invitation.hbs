<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Team Invitation - {{teamName}}</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      line-height: 1.6;
      color: #000;
      margin: 0;
      padding: 0;
      background: #fff;
    }
    .wrapper {
      max-width: 600px;
      margin: 60px auto;
      padding: 0 40px;
    }
    h1 {
      font-size: 24px;
      font-weight: 600;
      color: #000;
      margin: 0 0 30px 0;
    }
    h2 {
      font-size: 20px;
      font-weight: 600;
      color: #000;
      margin: 0 0 20px 0;
    }
    p {
      margin: 0 0 20px 0;
      color: #000;
    }
    .card {
      border: 1px solid #000;
      padding: 30px;
      margin: 30px 0;
    }
    .detail {
      margin: 15px 0;
      padding-bottom: 15px;
      border-bottom: 1px solid #e0e0e0;
    }
    .detail:last-child {
      border-bottom: none;
      padding-bottom: 0;
    }
    .detail strong {
      display: block;
      margin-bottom: 5px;
    }
    .button {
      display: inline-block;
      padding: 12px 32px;
      background: #000;
      color: #fff;
      text-decoration: none;
      margin: 30px 0;
    }
    .notice {
      border: 1px solid #000;
      padding: 20px;
      margin: 30px 0;
      background: #f9f9f9;
    }
    .url {
      word-break: break-all;
      color: #666;
      font-size: 12px;
      padding: 15px;
      background: #f5f5f5;
      margin: 15px 0;
    }
    .divider {
      height: 1px;
      background: #e0e0e0;
      margin: 40px 0;
    }
    .footer {
      color: #999;
      font-size: 12px;
      margin-top: 40px;
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <h1>Team Invitation</h1>

    <p>Hi {{inviteeName}},</p>

    <p><strong>{{inviterName}}</strong> has invited you to join their team on <strong>Calento</strong>!</p>

    <div class="card">
      <h2>{{teamName}}</h2>

      {{#if teamDescription}}
      <p>{{teamDescription}}</p>
      {{/if}}

      <div class="detail">
        <strong>Invited by</strong>
        {{inviterName}} ({{inviterEmail}})
      </div>

      <div class="detail">
        <strong>Your role</strong>
        {{role}}
      </div>

      <div class="detail">
        <strong>Invited on</strong>
        {{formatDate invitedAt}}
      </div>
    </div>

    <a href="{{acceptUrl}}" class="button">Accept Invitation</a>

    <p>If button doesn't work, copy this link:</p>
    <div class="url">{{acceptUrl}}</div>

    <div class="notice">
      <strong>Security Tip</strong>
      <p>If you don't recognize {{inviterName}} or didn't expect this invitation, you can safely ignore this email or decline the invitation.</p>
    </div>

    <div class="divider"></div>

    <p>The Calento Team</p>

    <div class="footer">
      <p>© {{year}} Calento</p>
    </div>
  </div>
</body>
</html>
