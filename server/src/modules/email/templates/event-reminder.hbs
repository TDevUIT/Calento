<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Reminder</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #1f2937;
            margin: 0;
            padding: 0;
            background-color: #ffffff;
        }
        .email-wrapper {
            max-width: 600px;
            margin: 0 auto;
            background: #ffffff;
        }
        .logo-section {
            text-align: center;
            padding: 40px 20px 20px;
        }
        .logo {
            max-width: 180px;
            height: auto;
        }
        .header {
            padding: 20px 40px;
            text-align: center;
        }
        .header h1 {
            color: #667eea;
            font-size: 28px;
            font-weight: 600;
            margin: 0;
        }
        .content {
            padding: 20px 40px;
        }
        .content p {
            margin: 15px 0;
            color: #4b5563;
        }
        .event-card {
            border: 2px solid #667eea;
            padding: 25px;
            margin: 25px 0;
            border-radius: 8px;
        }
        .event-card h2 {
            color: #1f2937;
            font-size: 22px;
            font-weight: 600;
            margin: 0 0 20px 0;
        }
        .event-detail {
            margin: 15px 0;
            color: #4b5563;
            padding-bottom: 15px;
            border-bottom: 1px solid #f3f4f6;
        }
        .event-detail:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }
        .event-detail strong {
            color: #1f2937;
            display: block;
            margin-bottom: 5px;
        }
        .button-container {
            text-align: center;
            margin: 35px 0;
        }
        .button {
            display: inline-block;
            padding: 14px 32px;
            background: #667eea;
            color: #ffffff;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            margin: 0 8px;
        }
        .button.secondary {
            background: #ffffff;
            color: #667eea;
            border: 2px solid #667eea;
        }
        .divider {
            height: 1px;
            background: #e5e7eb;
            margin: 30px 0;
        }
        .footer {
            padding: 30px 40px;
            text-align: center;
        }
        .footer p {
            color: #9ca3af;
            font-size: 13px;
            margin: 8px 0;
        }
    </style>
</head>
<body>
    <div class="email-wrapper">
        <!-- Logo -->
        <div class="logo-section">
            <img src="https://res.cloudinary.com/dq2z27agv/image/upload/q_auto,f_webp,w_1200/v1760519001/f6x9prkhpd10u7svw2il.webp" alt="Calento" class="logo" />
        </div>

        <!-- Header -->
        <div class="header">
            <h1>Event Reminder</h1>
        </div>

        <!-- Content -->
        <div class="content">
            <p>Hello,</p>
            
            <p>This is a reminder for your upcoming event:</p>
            
            <div class="event-card">
                <h2>{{title}}</h2>
                
                <div class="event-detail">
                    <strong>When</strong>
                    {{formatDate startTime}}
                </div>
                
                {{#if location}}
                <div class="event-detail">
                    <strong>Where</strong>
                    {{location}}
                </div>
                {{/if}}
                
                {{#if description}}
                <div class="event-detail">
                    <strong>Details</strong>
                    {{description}}
                </div>
                {{/if}}
            </div>
            
            <div class="button-container">
                <a href="{{eventUrl}}" class="button">View Event</a>
                <a href="{{calendarUrl}}" class="button secondary">Open Calendar</a>
            </div>

            <div class="divider"></div>
            
            <p>Best regards,<br><strong>The Calento Team</strong></p>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>© {{year}} Calento. All rights reserved.</p>
            <p>This is an automated reminder from your calendar.</p>
        </div>
    </div>
</body>
</html>
